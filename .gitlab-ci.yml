stages:
  - install      # Install dependencies
  - prettier     # Run Prettier Check
  - eslint       # Run ESLint Fix
  - test         # Run Tests
  - build_android        # Build Android APK

install_dependencies:
  stage: install
  image: node:20
  script:
    - echo "📦 Installing dependencies..."
    - npm install
    - echo "✅ Dependencies installed!"

prettier_check:
  stage: prettier
  image: node:20
  script:
    - echo "📦 prettier checking..."
    - npm run prettier:check
    - echo "✅ prettier checked!"

eslint_check:
  stage: eslint
  image: node:20
  script:
    - echo "📦 Installing dependencies..."
    - npm ci        # or npm install
    - echo "📦 Running ESLint..."
    - npm run lint:fix
    - echo "✅ ESLint done!"   

test_check:
  stage: test
  image: node:20
  script:
    - echo "📦 test checking..."
    - npm ci      # or npm install
    - npm run test
    - echo "✅ test checked!"

build_android_check:
  stage: build_android
  image: reactnativecommunity/react-native-android
  before_script:
    - echo "Preparing Android environment..."
    - export PATH=$PATH:$ANDROID_HOME/cmdline-tools/latest/bin:$ANDROID_HOME/platform-tools
    - npm ci
    - npm install -g firebase-tools
  script:
    - echo "📦 Building Android Debug APK..."
    - cd android
    - ./gradlew assembleDebug
    - echo "✅ Debug APK built!"
    - cd ..
    - echo "🚀 Uploading APK to Firebase App Distribution..."
    - echo "🔄 Renaming APK..."
    - mv android/app/build/outputs/apk/debug/app-debug.apk android/app/build/outputs/apk/debug/app_git_lab-debug.apk
    - echo "✅ APK renamed to app_git_lab-debug.apk"
    - >
      firebase appdistribution:distribute android/app/build/outputs/apk/debug/app_git_lab-debug.apk
      --app ${FIREBASE_APP_ID}
      --token ${FIREBASE_TOKEN}
      --groups "TESTERS_REELBOOK"
    - echo "✅ APK uploaded to Firebase App Distribution!"
        
